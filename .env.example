# Database Configuration
DATABASE_URL="postgresql://postgres:password@localhost:5432/ticketing_system?schema=public"

# Application
NODE_ENV=development
PORT=3000

# Database Configuration
DB_SCHEME=postgresql
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=ticketing_system
DATABASE_URL="${DB_SCHEME}://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}"

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT Configuration
JWT_SECRET=your-secret-key-change-this-in-production
JWT_EXPIRES_IN=3600

# Locking Configuration
LOCK_TTL_SECONDS=30
RESERVATION_TIMEOUT_MINUTES=10OUT=20

# Optional: Prisma Studio Port
# PRISMA_STUDIO_PORT=5555
